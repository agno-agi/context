manifest:
  # --- Agents ---

  context:
    description: "Your self-hosted context agent."
    labels: ["alter ego"]
    quick_prompts:
      - "What's my day like?"
      - "Tell me about your architecture"
      - "Met Kyle from Agno, wants a partnership. Follow up next week."

  # --- Workflows ---

  queue-reminders:
    description: "Push reminders into your inbound queue."
    labels: ["scheduled", "hourly"]

  daily-digest:
    description: "Get your daily rundown as a DM in Slack."
    labels: ["scheduled", "daily"]

  weekly-digest:
    description: "Get your week-ahead plan as a DM in Slack."
    labels: ["scheduled", "weekly"]
